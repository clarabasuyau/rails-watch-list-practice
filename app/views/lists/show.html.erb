<div class="cards">
  <h2 class="text-center mb-5"><%= @list.name %></h2>
    <% @list.bookmarks.each do |bookmark| %>
      <div class="card-product mb-3">
        <%= image_tag bookmark.movie.poster_url, alt: "poster", width: 200 %>
          <div class="card-product-infos">
            <h3><%= bookmark.movie.title %></h3>
            <p><%= bookmark.movie.rating %> /10</p>
            <p><%= link_to "🗑",bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></p>
          </div>
      </div>
      <% end %>
</div>

<p type="button" class="ms-5 btn btn-danger"><%= link_to "add a movie", new_list_bookmark_path(@list) %></p>
